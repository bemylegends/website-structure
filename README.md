# Legends website

A minimal Next.js wrapper for the supplied interactive React prototype. The prototype styles live in `app/globals.css`; no CSS framework is used.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000. For a production build, run `npm run build` and `npm start`.
